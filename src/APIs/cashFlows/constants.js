const COLLATION_NAME = 'cashFlows';
const LOCAL_NAME = 'cashFlow';
const PER_PAGE_DEFAULT = 30;
const inOut = {
  INPUT: false,
  OUTPUT: true
};

module.exports = {
  COLLATION_NAME,
  LOCAL_NAME,
  PER_PAGE_DEFAULT,
  inOut
};
